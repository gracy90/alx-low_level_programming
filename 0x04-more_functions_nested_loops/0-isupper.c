#include "main.h"

/**
 * _isupper - chech if a letter is upper
 * @x: number to be checked
 * Return 1 upper 0r 0 for any else
 */

int_isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
